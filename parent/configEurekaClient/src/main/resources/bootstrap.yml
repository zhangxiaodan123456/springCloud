server:
  port: 7080
spring:
  application:
    name: configclient
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        service-id: configserver
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7071/eureka/

